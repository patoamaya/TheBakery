import React from 'react'

const Delete = ({data}) => {
const {tamano, nombre, categoria} = data
 {
    return(
                <div>
                <h2>{nombre}</h2>
                </div>
            )
        }
    }

        
    

export default Delete
